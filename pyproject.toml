[project]
name = "dossier"
version = "0.1.0"
description = "SaaS due diligence engine — Claude Code native"
requires-python = ">=3.9"
license = "MIT"

[project.optional-dependencies]
scripts = [
    "python-whois>=0.9.0",
    "arxiv>=2.0.0",
]
